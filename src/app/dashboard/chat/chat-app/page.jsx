import ChatWrapper from "./components/ChatWrapper";

const page = () => {
      

    return (
        <div className="flex flex-1 overflow-hidden">
             <ChatWrapper />
        </div>
    );
};

export default page;